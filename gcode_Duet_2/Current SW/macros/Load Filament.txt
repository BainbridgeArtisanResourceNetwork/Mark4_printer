M98 P"0:/macros/load_unload_filaments/unload_current_filament.g"
var filament_loaded = false

if true    ; needed to keep Cancel from cancelling the whole macro
  M291  S3 T10 R"Load Filament.g file" P"To load PLA, press OK.  Press cancel to move to other filaments."
  M98 P"0:/macros/load_unload_filaments/load_PLA.g"
  set var.filament_loaded = true

if not filament loaded
  if true    ; needed to keep Cancel from cancelling the whole macro
    M291  S3 T10 R"Load Filament.g file" P"To load PETG, press OK.  Press Cancel to move to other filaments."
    M98 P"0:/macros/load_unload_filaments/load_PETG.g"
    var "filament_loaded" = true

if not filament loaded
  if true    ; needed to keep Cancel from cancelling the whole macro
    M291  S3 T10 R"Load Filament.g file" P"To load TPU, press OK.  Press Cancel to Exit."
    M98 P"0:/macros/load_unload_filaments/load_TPU.g"
    var "filament_loaded" = true